/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    host: "localhost",
    port: 3306,
    user: "root",
    password: "GraNiNo2019",
    database: "fairhold",
  },
};

export default nextConfig;
